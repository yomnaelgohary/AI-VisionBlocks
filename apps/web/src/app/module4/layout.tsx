import type { ReactNode } from "react";

export const metadata = {
  title: "Module 4 · Building, training, and evaluating models",
};

export default function Module4Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
